# Example environment variables for local development and production
# Copy this file to .env and fill in the values (DO NOT commit .env)

# MongoDB connection string (MongoDB Atlas recommended for production)
# Replace <DB_USER> and <DB_PASSWORD> with your Atlas credentials
MONGO_URI=mongodb+srv://<DB_USER>:<DB_PASSWORD>@cluster0.stsxxsq.mongodb.net/Portfolio?retryWrites=true&w=majority

# Node environment
NODE_ENV=development

# Port (optional; Render provides PORT automatically)
PORT=3000
